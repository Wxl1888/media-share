version: '3.8'

services:
  media-share:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - FRONTEND_URL=http://localhost:3000
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
